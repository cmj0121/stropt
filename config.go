package stropt

const (
	// the project name
	PROJ_NAME = "stropt"
	// the version meta
	MAJOR = 0 // bump when API changed
	MINOR = 1 // bump when new feature add
	MACRO = 0 // bump when bug fixed
)
